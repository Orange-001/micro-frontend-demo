export function HomeView() {
  return (
    <div className="view">
      <h2>首页</h2>
      <p style={{ opacity: 0.85 }}>
        这是 React 子应用。为了减少与容器前缀冲突，这里使用 <code>HashRouter</code>。
      </p>
    </div>
  );
}

