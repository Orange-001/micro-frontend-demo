services:
  host:
    build:
      context: .
      dockerfile: apps/host/Dockerfile
      args:
        VITE_VUE_ENTRY: "http://localhost:8081/"
        VITE_REACT_ENTRY: "http://localhost:8082/"
    ports:
      - "8080:80"
    depends_on:
      - vue-child
      - react-child

  vue-child:
    build:
      context: .
      dockerfile: apps/vue-child/Dockerfile
      args:
        VITE_APP_BASE: "http://localhost:8081/"
    ports:
      - "8081:80"

  react-child:
    build:
      context: .
      dockerfile: apps/react-child/Dockerfile
      args:
        VITE_APP_BASE: "http://localhost:8082/"
    ports:
      - "8082:80"

